import { Routes } from '@angular/router';
import { CompleteSignInComponent } from './complete-sign-in.component';

export const COMPLETE_SIGN_IN_ROUTES: Routes = [
    {
        path: '',
        component: CompleteSignInComponent,
    },
];
